# Epigenetic clock reproduction — Python 3.11+
# Install: python -m venv .venv && .venv\Scripts\activate  (Windows)
#          pip install -r requirements.txt
#
# Listed packages are imported directly by notebooks or pulled as explicit pip install deps there.
# Pin upgrades intentionally when re-validating the full notebook suite.

# --- Core numerics & tables ---
numpy==1.26.4
pandas==2.2.3

# --- Parquet / Arrow I/O (notebooks reference pyarrow in pip installs) ---
pyarrow==17.0.0

# --- Statistics & ML ---
scipy==1.13.1
scikit-learn==1.5.2

# --- Plotting ---
matplotlib==3.9.2
seaborn==0.13.2

# --- GEO ---
GEOparse==2.0.3

# --- Serialization / spreadsheets ---
joblib==1.4.2
openpyxl==3.1.5

# --- Jupyter (notebook UI; start with: jupyter notebook) ---
ipykernel==6.29.5
notebook==7.3.2
